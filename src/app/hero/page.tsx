"use client";

import React from "react";
import Hero from "@/components/hero"; // Make sure Hero component is saved in components folder

const Page = () => {
  return <Hero />;
};

export default Page;
